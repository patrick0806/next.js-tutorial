//É neste arquivo que os importes de estilo global devem ser importados
import '../styles/global.css'
export default function App({ Component, pageProps }) {
    return <Component {...pageProps} />
  }